{
  "name": "functions",
  "description": "Optional Firebase Functions for Upgrader",
  "scripts": {
    "lint": "echo no-lint",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": { "node": "20" },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^5.1.1"
  },
  "private": true
}
